import { Policy } from './policy';

describe('Policy', () => {
  it('should create an instance', () => {
    expect(newPolicy()).toBeTruthy();
  });
});

function newPolicy(): any {
  throw new Error('Function not implemented.');
}
